{
  "name": "cadence-base",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend-vite",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend-vite",
    "dev": "npm run dev --workspace=frontend-vite",
    "start": "npm run preview --workspace=frontend-vite"
  }
}